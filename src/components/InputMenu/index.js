import React from 'react'
const InputMenu=({value,onChange,placeholder})=><input type='text' className='input-menu' placeholder={placeholder} onChange={onChange} value={value} />
export default InputMenu;